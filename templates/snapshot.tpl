<!doctype html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{{title}}</title>
    <style type="text/css">
        body {
            margin:0;
            padding:0;
            height:100%;
            overflow:hidden;
        }
        iframe {
            overflow:hidden;
            margin:0;
            padding:0;
            border:none;
        }
    </style>
</head>
<body>

    <iframe src="{{src}}" scrolling="no" height="100%" width="{{width}}"></iframe>

</body>
</html>